<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.engcria.ruifma_mobile">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RuIfmaMobile"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".rotinas.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/Theme.RuIfmaMobile.NoActionBar"></activity>
        <activity android:name=".rotinas.CadastroActivity"></activity>
        <activity android:name=".rotinas.LoginActivity" />
        <activity android:name=".rotinas.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>